// Double an integer
//P: we are working with integers. The goal is to have it doubled. 
// R: console.log the result
// E:
// 50 -----> 100
// 100 -----> 200

// p:

// write a function
function integerTo(i) {

    // let i be the integer, double it then return
     return (i * 2);
  }
  // print answer in the console
  console.log(integerTo(50))
  


